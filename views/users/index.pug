extends ../layouts/common
block content

	h1 Users

	a(href="/users/create") New a user

	form(action="/users/search", method: "GET")
		input(type="text", name="q")
		button Search
	ul 	
		each user in users
			li
				span= user.name
				span &nbsp
				a(href="/users/" +user.id) View